import { defineConfig } from "vite";

// https://vitejs.dev/config/
export default defineConfig({
  build: {
    outDir: "build",
  },
  base: "https://302chanwoo.github.io/react-router-page-action",
});
